PORT=3000
# CF_API_TOKEN=cf_...    # Paste your Cloudflare API token here (do NOT commit the real token)
# CF_ACCOUNT_ID=...      # Cloudflare Account ID
# CF_ZONE_ID=...         # Cloudflare Zone ID
ACCESS_TOKEN=
ACCESS_TOKEN_TIMESTAMP=
ACCESS_TOKEN_EXPIRES_AT=
TELEGRAM_BOT_TOKEN=
TELEGRAM_MASTER_ID=
TFA_SECRET=
TFA_SECRET_EXPIRES_AT=
PASSWORD_HASH=
PASSWORD_SALT=
ALLOW_UNAUTHENTICATED=false

# Rename this file to `.env` and fill values. Keep `.env` out of version control.
